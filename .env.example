# Hit FM 官网 Ajax 请求所需 Cookie。
# 示例格式：HITFM_COOKIE=PHPSESSID=你的会话值
# 如果接口不强制要求 Cookie，可以先留空。
HITFM_COOKIE=

# 地域限制（可选）。
# 设置为 1 启用地域限制：大陆 IP 自动屏蔽指定地区电台，海外 IP 无限制。
# 前置 Cloudflare Tunnel 时自动通过 CF-IPCountry 头判断国家。
# GEO_RESTRICT=
# 被屏蔽的地区 tag，逗号分隔，默认 TW（台湾）。
# GEO_BLOCKED_REGIONS=TW
